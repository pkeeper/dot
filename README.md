Installation
============

    $ git clone ... ~/dot
    $ cd ~/dot
    $ python bootstrap.py

Adding new configs
==================

* Copy config/folder into repo.
* Edit `bootstrap.py`. Add paths for new config into `symlinks` list.
